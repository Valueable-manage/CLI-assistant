# 个人 AI 助手项目 —— 学习路线与进阶方向

> 基于当前 CLI-assistant 项目，记录接下来的功能迭代方向和个人技术学习路线。
> 更新时间：2026-04-08

---

## 当前项目已掌握的能力

完成这个项目，你已经实际用过了以下技术：

| 技术 | 在项目中的体现 |
|------|--------------|
| FastAPI | 后端 API、流式响应、文件上传 |
| React + TypeScript | 前端状态管理、流式渲染 |
| OpenAI Tool Use | Agent 核心循环（LLM 自主调用工具） |
| RAG | ChromaDB 向量检索 + 文档解析 |
| SQLite | 会话持久化 |
| Streaming | NDJSON 流式输出，实时显示回复 |
| Prompt Engineering | System Prompt 行为约束 |

这已经覆盖了 AI 应用开发的完整链路，是很好的基础。

---

## 一、项目接下来可以加的功能

按优先级排列，从易到难。

### 阶段一：完善体验（难度 ⭐）

#### 1. Prompt 模板 / 快捷指令
- **是什么**：预设常用任务按钮，点一下自动填入输入框
- **例子**：「翻译成英文」「帮我总结」「写一个 Python 脚本」
- **实现**：纯前端，在 InputBox 上方加快捷按钮，点击填充 textarea
- **学到什么**：前端组件设计，理解 Prompt 复用

#### 2. 对话导出
- **是什么**：把当前对话保存为 Markdown 或复制到剪贴板
- **实现**：前端把 messages 数组拼成 Markdown 字符串，用 Blob 下载
- **学到什么**：前端文件操作，理解数据格式转换

#### 3. 历史对话搜索
- **是什么**：在侧边栏搜索过去说过的内容
- **实现**：后端加 `GET /api/search?q=关键词`，SQLite `LIKE` 查询
- **学到什么**：SQL 查询设计，搜索功能实现

---

### 阶段二：能力扩展（难度 ⭐⭐）

#### 4. 接入本地模型（Ollama）
- **是什么**：在自己电脑上运行开源模型（Qwen、Llama），不花 API 费用
- **实现**：安装 Ollama，只需修改 `.env` 中的 `OPENAI_BASE_URL` 和 `OPENAI_MODEL`
  ```env
  OPENAI_BASE_URL=http://localhost:11434/v1
  OPENAI_MODEL=qwen2.5:7b
  ```
- **学到什么**：模型部署概念，理解 OpenAI 兼容接口，本地 vs 云端的权衡

#### 5. SQL 数据库查询工具
- **是什么**：让 Agent 能查询本地 SQLite 数据库，回答数据相关问题
- **实现**：新增 `tools/db_query.py`，注册到工具列表
- **学到什么**：Tool 开发流程，SQL 安全（防注入），Agent 工具扩展

#### 6. 长期记忆（跨会话）
- **是什么**：Agent 记住你的偏好，比如「我喜欢用 Python」「我是初学者」
- **实现**：每次对话结束后，让 LLM 提取关键信息存入 SQLite，下次对话注入 Prompt
- **学到什么**：记忆管理设计，Prompt 动态构建

---

### 阶段三：工程进阶（难度 ⭐⭐⭐）

#### 7. 可观测性（LangSmith）
- **是什么**：可视化追踪每次 LLM 调用的输入、输出、Token 消耗、耗时
- **为什么重要**：现在只能靠 `print` 调试，真实项目需要追踪工具
- **实现**：接入 LangSmith SDK，给 Agent 每次调用打标签
- **学到什么**：AI 应用的可观测性，调试复杂 Agent 的方法

#### 8. 网页浏览工具
- **是什么**：不只搜索，能真正打开一个网页、提取内容、点击交互
- **实现**：用 `crawl4ai` 或 `playwright` 新增浏览器工具
- **学到什么**：无头浏览器，内容提取，Agent 与真实网页交互

#### 9. 语音输入 / 输出
- **是什么**：用麦克风说话输入，AI 语音播报回复
- **实现**：前端 Web Speech API 转文字，后端接文字走现有流程；回复用 TTS 播放
- **学到什么**：浏览器 API，多模态交互

---

## 二、值得深入学习的技术方向

### 方向一：Agent 进化（最重要）

**你现在掌握的**：
```
简单 Tool Use Loop
用户问题 → LLM → 调工具 → LLM → 调工具 → 最终回复
（线性，一条路走到底）
```

**下一步学习**：

#### ReAct 模式（Reasoning + Acting）
- 让 LLM 在每次行动前先写出"思考过程"，再决定调哪个工具
- 比直接 Tool Use 更可解释，错误更容易排查
- 关键论文：《ReAct: Synergizing Reasoning and Acting in Language Models》

#### LangGraph（重点推荐）
- **是什么**：把 Agent 从线性流程升级为"有状态的工作流图"
- **能做什么**：分支判断（成功走A路/失败走B路）、循环重试、并行执行多个子任务
- **实践**：用 LangGraph 重写现在的 `agent/core.py`
- **资源**：LangGraph 官方文档 + LangChain Academy 课程（免费）

```
线性 Agent（现在）：A → B → C → D → 结果
LangGraph Agent：
        ┌→ B1（搜索）→┐
A → 判断            合并 → 结果
        └→ B2（读文件）→┘
```

#### Multi-Agent 系统
- 多个专门的 Agent 协作完成任务
- 例如：一个 Agent 专门搜索，一个专门写代码，一个负责审查结果
- 这是目前大厂 AI 产品的主流架构（AutoGen、CrewAI）

---

### 方向二：RAG 进阶

**你现在掌握的**：基础 RAG（文档切块 → 向量 → 检索 → 塞入 Prompt）

**基础 RAG 的问题**：
- 只靠语义相似度，关键词匹配差
- 检索到的内容排序不够准确
- 复杂问题需要多跳推理（A 依赖 B，B 依赖 C）

**进阶方向**：

| 技术 | 解决的问题 | 难度 |
|------|-----------|------|
| Hybrid Search | 向量检索 + BM25 关键词检索混合，提升召回率 | ⭐⭐ |
| Reranking | 检索后用交叉编码器重新排序，提升精度 | ⭐⭐ |
| GraphRAG | 微软开源，知识图谱增强，适合复杂文档关系 | ⭐⭐⭐ |
| HyDE | 先让 LLM 生成假设答案，再用答案去检索 | ⭐⭐ |

**RAG 评估（重要）**：
- 工具：**RAGAS**（开源，专门评估 RAG 质量）
- 评估指标：Faithfulness（忠实度）、Answer Relevancy（相关性）、Context Recall（召回率）
- **为什么要学**：不评估就不知道 RAG 到底好不好，优化无从下手

---

### 方向三：模型微调（Fine-tuning）

**什么时候需要微调**（先搞清楚，避免走弯路）：
```
❌ 不需要微调的情况：
  - 想让模型知道新知识 → 用 RAG
  - 想改变回答风格 → 用 Prompt Engineering
  - 任务通用性强 → 用大模型 API

✅ 需要微调的情况：
  - 想让模型固定输出某种格式（如特定 JSON 结构）
  - 特定垂直领域，大模型表现差（如医疗、法律专业术语）
  - 需要降低推理成本（微调小模型替代大模型）
```

**学习路线**：
1. 理解 Full Fine-tuning vs LoRA vs QLoRA 的区别
2. 用 **LLaMA-Factory** 或 **Unsloth** 在本地微调 Qwen2.5-7B
3. 准备数据集（对话格式），理解数据质量比数量更重要
4. 评估微调效果，与基础模型对比

---

### 方向四：可观测性与工程化

这是从"能跑起来"到"生产可用"的关键差距。

| 工具 | 用途 |
|------|------|
| **LangSmith** | 追踪 LLM 调用链，可视化 Token 消耗、延迟 |
| **Phoenix（Arize）** | 开源版，本地部署，RAG 质量分析 |
| **Prometheus + Grafana** | 服务监控，API 响应时间、错误率 |
| **Docker** | 容器化部署，一键启动不依赖本地环境 |

---

## 三、推荐学习顺序

```
现在（已完成当前项目）
    │
    ▼
【第1步】给项目加 Prompt 模板 + 对话导出        预计 1-2 天
  目标：巩固前端开发，增加项目实用性
    │
    ▼
【第2步】接入 Ollama，跑本地 Qwen2.5           预计 1 天
  目标：理解模型部署，体验本地 vs 云端差异
    │
    ▼
【第3步】学习 LangGraph 基础概念               预计 1-2 周
  目标：理解复杂工作流，用 LangGraph 重构 Agent
  资源：LangChain Academy（免费在线课程）
    │
    ▼
【第4步】RAG 进阶：加入 Hybrid Search          预计 1 周
  目标：提升知识库检索质量，学习 RAGAS 评估
    │
    ▼
【第5步】给项目加 LangSmith 可观测性           预计 2-3 天
  目标：学会调试复杂 Agent，工程化思维
    │
    ▼
【第6步】LoRA 微调一个小模型                   预计 2-3 周
  目标：理解模型训练，感受微调前后的差异
    │
    ▼
【第7步】Multi-Agent 系统（进阶）              预计 持续学习
  目标：设计更复杂的 AI 应用架构
```

---

## 四、推荐学习资源

### 文档 / 教程
- [LangGraph 官方文档](https://langchain-ai.github.io/langgraph/) — Agent 工作流
- [LangChain Academy](https://academy.langchain.com/) — 免费课程，包含 LangGraph
- [RAGAS 文档](https://docs.ragas.io/) — RAG 评估框架
- [Ollama 官网](https://ollama.ai/) — 本地模型部署
- [LLaMA-Factory](https://github.com/hiyouga/LLaMA-Factory) — 微调工具（国人开发）

### 论文（不需要全看，了解核心思想即可）
- ReAct: Reasoning and Acting in Language Models
- RAG: Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks
- LoRA: Low-Rank Adaptation of Large Language Models

### 跟踪动态
- [AI产品榜](https://top.aibase.com/) — 了解最新 AI 产品
- Hugging Face Daily Papers — 每天推送重要论文
- LangChain Blog — Agent 和 RAG 领域最新进展

---

## 五、核心学习原则

根据你的学习方式，这些原则很重要：

1. **先做再懂**：跑通代码，再回头理解原理，比先看理论更高效
2. **改造现有项目**：所有新技术都尽量在这个项目里实践，而不是从零开始
3. **记录踩坑**：遇到的每个报错和解决方法都值得记录，以后一定还会遇到
4. **关注 Token 成本**：AI 应用的工程化核心之一就是控制成本，这个意识要早建立
5. **不要追所有新技术**：AI 领域每天都有新东西，按路线图走，不要跳来跳去

---

*最后更新：2026-04-08*
*当前项目版本：CLI-assistant v0.1.0*
